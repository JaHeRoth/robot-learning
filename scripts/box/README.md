# scripts/box

Orchestration and eval-farm scripts generated by Claude Code under my direction
during the training block (cloud-box eval daemon, 5000-seed fragment evals,
setup checklist); kept for reproducibility. `my_act_policy.py` and
`my_eval_frag.py` are parked here until I rewrite them myself.

Everything outside this folder is written by me unless noted.
